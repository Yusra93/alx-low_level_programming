#include <stdio.h>
#include "main.h"

/**
 * _isupper - checks for uppercase character.
 * Description: prints the date
 * @c: character to check
 * Return: int
 */

int _isupper(int c)
{
	if (c >= 65 && c <=)
		return (1);
	else
		return (0);
}

