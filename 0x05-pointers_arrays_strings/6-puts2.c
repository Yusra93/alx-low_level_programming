#include <stdio.h>
#include "main.h"
#include <string.h>

/**
 * puts2 - function that prints every other character of a string.
 * @str: address to string
 * Return: void
 */

void puts2(char *str)
{
	int len = strlen(s);
	int i = 0;

	while (i+=2)
	{
	_putchar(s[i]);
	i++;
	}
}

