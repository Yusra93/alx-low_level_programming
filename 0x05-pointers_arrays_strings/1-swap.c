#include <stdio.h>
#include "main.h"

/**
 * swap_int : function that swaps the values of two integers.
 * @a: address
 * @b: address
 * Return: void
 */

void swap_int(int *a, int *b)
{
	int *p1, *p2;

	p1 = a;
	p2 = b;
	p1 = b;
	p2 = a;
}

